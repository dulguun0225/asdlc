# Handbook

Rules this handbook keeps about itself:

1. Every file under `notes/` ends with an `## Open` section. An empty one, stated as empty, satisfies this.
2. Every relative link in every file resolves to a file that exists.
3. Any count of notes written in this README matches the number of files under `notes/`.

This handbook holds four notes. Start at [alpha](notes/001-alpha.md).
