[tools]
# Exact, not the "26" major: two machines resolving a version differently is
# the kind of difference that shows up as a behaviour change in the toolchain.
# The same pin as tools/skills-harness/mise.toml — one toolchain for the
# repository (ADR-0041).
#
# node is the whole toolchain here: check-specs.mjs and
# statemodel-to-mermaid.mjs are built-ins-only and run on any maintained Node.
node = "26.5.1"
